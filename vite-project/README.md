<!-- @format -->

# Author

Kamilla Salehova

# Vite React Todo List

## Live demo:

Coming soon. <!--http://localhost:5173/ -->

# ScreenShot:

## Features

Add new todos.

Mark todos as completed.

Delete todos.

Filter todos by all / active / completed.

Responsive layout for mobile and desktop.

# Tech stack

Vite + React

JavaScript

CSS

## Getting Started

### Prerequisites

## Node.js and npm installed

<!-- will do it later -->

### Installation

```bash
git clone https://github.com/your-username/todo-list.git
cd todo-list
npm install
npm run dev
```

Open the URL shown in the terminal (usually http://localhost:5173).
Adjust folder name/commands to your project.

# Usage instructions

Explain how to use the app from the user’s point of view.

Example:

Type a task into the input and press Enter to add it.

Click the checkbox to mark a todo as completed.

Click the trash icon to delete a todo.

Use the filter buttons to show all / active / completed todos.

# Project status / future ideas (optional)

Nice for bootcamp projects.

Example:

Current status: MVP complete.

Next steps:

Add localStorage persistence.

Add due dates and priority.

Add drag-and-drop reordering.

## Learning goals / what you practiced (optional)

Great for portfolio and Code the Dream.

Example:

Practiced React component structure and props.

Learned how to manage state with useState.

Practiced using Vite for fast dev setup.

Used Git and GitHub for version control.

9. License / credits (optional)
   Add a simple license line (MIT, etc.) if you want.

Credit any tutorial or design inspiration you followed.

<!-- # React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Oxc](https://oxc.rs)
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/)

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.
 -->